# GPU server: LoRA inference only (no compile / CodeBLEU).
#   pip install -r requirements-lora-server.txt
torch>=2.2.0
transformers>=4.45.0
peft>=0.11.0
accelerate>=0.33.0
safetensors>=0.4.0
tqdm>=4.66.0
bitsandbytes>=0.43.0; platform_system != "Darwin"
